# Runtime dependencies. Mirrors [project.dependencies] in pyproject.toml so that
# Streamlit Community Cloud (which only reads requirements.txt) installs the same set.
#
# For local development prefer:  pip install -e ".[dev]"

streamlit>=1.50
llama-index-core>=0.14,<0.15
llama-index-llms-openai>=0.7,<0.8
llama-index-llms-anthropic>=0.11,<0.12
llama-index-llms-google-genai>=0.10,<0.11
httpx>=0.27
pydantic>=2.7
pydantic-settings>=2.3
pydeck>=0.9
flexpolyline>=0.1
